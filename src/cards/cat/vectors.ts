/**
 * @author Chachako
 */
export class CatCardVectors {
    constructor(
        readonly illustration: string,
        readonly kotlinCode: string,
        readonly rustCode: string,
    ) {
    }
}